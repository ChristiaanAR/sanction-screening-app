import { Routes } from '@angular/router';
import { SanctionScreenFormComponent } from './sanction-screen-form/sanction-screen-form.component';

export const routes: Routes = [
    {
        path: '',
        component: SanctionScreenFormComponent,
    },
];
